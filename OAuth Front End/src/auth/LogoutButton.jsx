import React from "react";

const LogoutButton = () => {
  const logout = async () => {};

  return (
    <button className="Login-button" onClick={() => logout()}>
      Log out
    </button>
  );
};

export default LogoutButton;
